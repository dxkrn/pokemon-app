const Navbar = () => (
    <nav className="absolute top-0 left-0 w-full z-50 bg-transparent px-4 py-8">
        <div className="max-w-screen-xl mx-auto flex items-center">
            <img src="/assets/logo.svg" alt="Logo" className="h-10 md:h-16" />
        </div>
    </nav>
);

export default Navbar;
